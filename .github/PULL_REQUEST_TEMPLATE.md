## What

## Why

## Checklist
- [ ] `npm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes
- [ ] No provider SDK imports outside `src/providers/` (golden rule)
- [ ] Tests added for new behavior
- [ ] Docs updated if user-facing
